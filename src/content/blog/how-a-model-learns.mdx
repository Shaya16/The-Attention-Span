---
title: "איך מודל לומד? הסיפור המלא של אימון"
subheading: "הסבר מלא על training, gradient descent ו-backprop"
description: "כולם אומרים 'אימנו את המודל על מיליארדי דוגמאות'. אף אחד לא מסביר מה זה אומר בפועל. מפרמטרים, דרך loss וגרדיאנטים, ועד backprop - כל מה שקורה כשמודל לומד, כולל המתמטיקה שרוב ההסברים מדלגים עליה."
pubDate: "2026-05-31"
tags: ["training", "backpropagation", "gradient-descent", "neural-networks", "LLM"]
image: {"src": "../../assets/blog/how-a-model-learns/frame-11.png", "alt": ""}
---

import ParameterDimmers from '../../components/viz/ParameterDimmers'

import Explainer from '../../components/blog/Explainer.astro'

import TrainingLoop from '../../components/viz/TrainingLoop'

import Callout from '../../components/blog/Callout.astro'

import BackpropDiagram from '../../components/blog/BackpropDiagram.astro'

import BackpropGraph from '../../components/viz/BackpropGraph'

import LossLandscape from '../../components/viz/LossLandscape'

import LossCurve from '../../components/viz/LossCurve'

import GradientDescent from '../../components/viz/GradientDescent'

import PretrainFinetune from '../../components/viz/PretrainFinetune'

import PullQuote from '../../components/blog/PullQuote.astro'

**"אימנו את המודל על מיליארדי דוגמאות."**
משפט שמופיע בכל כתבה על AI. אבל אף אחד לא מסביר מה זה אומר בפועל.

בפוסט הזה נחקור בדיוק את הנושא הזה, מה זה אומר אימון, איך מודל מתאמן וגם קצת מתמטיקה.

## מה זה בעצם מודל?

לפני שנדבר על האימון, צריך להבין מה אנחנו בכלל מאמנים.

בניגוד לתוכנה רגילה, שבה מתכנת כתב חוקים מפורשים ("אם המשתמש שאל X, תחזיר Y"), מודל שפה הוא לא אוסף של חוקים. הוא ערימה ענקית של מספרים שקוראים להם **פרמטרים**.

אפשר לחשוב על כל פרמטר כמו דימר של אור. מספר שכשמזיזים אותו, זה משפיע קצת על מה שיוצא בסוף. רק שבניגוד לדימר אמיתי, הערך יכול להיות גם שלילי — לא רק "יותר" או "פחות". מודל כמו GPT או קלוד מכילים מעל טריליון כאלה, כולם מחוברים ושינוי באחד משפיע על התוצאה הסופית.

רק שימו לב: הדימרים לא יוצרים את התשובה לבדם. הם קובעים איך הקלט שאתם נותנים הופך לפלט. אותם דימרים בדיוק, קלט שונה — תשובה שונה.

כל "הידע" שמודל צבר, על שפה, על עובדות, על איך לענות על שאלות, שמור בתוך הערכים של הפרמטרים האלה. לא בקוד. במספרים.

<ParameterDimmers client:visible />

## אז מה זה אומר אימון?

אימון הוא התהליך שמוצא את הערכים הנכונים של הפרמטרים האלה.

הפרמטרים מתחילים כמספרים אקראיים ולכן מודל לא מאומן פולט ג'יבריש מוחלט. האימון הוא מה שדוחף אותם, צעד אחר צעד, מאקראיות לערכים שימושיים.

ואף אחד לא מגדיר אותם ידנית. המודל מצא אותם בעצמו, דרך לולאה אחת פשוטה שחוזרת על עצמה המון פעמים:

1. מראים למודל דוגמה ומבקשים שינחש את המשך הטקסט. זה נקרא **forward pass**
2. מודדים כמה הוא טעה. המספר הזה נקרא **loss**
3. מעדכנים את הפרמטרים בכיוון שיקטין את הטעות
4. חוזרים

<Explainer term="forward pass">
  המעבר "קדימה" ברשת: מזינים קלט, הוא עובר דרך כל הפרמטרים שכבה אחרי שכבה, ובסוף יוצא ניחוש. קוראים לזה "forward" כי המידע זורם מהקלט אל הפלט. ה-backward pass, שנגיע אליו בהמשך, זורם בדיוק בכיוון ההפוך.
</Explainer>

<TrainingLoop client:visible />

## Loss - המדד לכמה המודל טעה

ה-loss הוא מספר אחד שמסכם כמה המודל טעה. המטרה של כל האימון היא להוריד אותו כמה שאפשר.

אחת הדרכים הנפוצות למדוד טעות נקראת **Mean Squared Error**:

$$
L = \frac{1}{n}\sum_{i=1}^{n}(\hat{y}_i - y_i)^2
$$

בשפה פשוטה: לוקחים את ההפרש בין מה שניחשנו ($\hat{y}$) למה שנכון ($y$), מעלים בריבוע, וממצעים על כל הדוגמאות.

למה בריבוע? שתי סיבות:&#x20;
1\. טעויות גדולות מקבלות עונש גדול יותר באופן לא לינארי, וכל הערכים יוצאים חיוביים
2\. שגיאה של 3+ ושגיאה של 3- חמורות באותה מידה.

<Callout type="note" title="MSE מול cross-entropy">
  ה-MSE מתאים בעיקר כאשר הפלט הוא ערך מספרי רציף. לעומת זאת, מודל שפה מנבא את הטוקן הבא מתוך אוצר מילים שלם, ולכן משתמשים בפונקציית הפסד מתאימה יותר בשם **cross-entropy**. גם כאן המטרה היא למדוד עד כמה תחזית המודל רחוקה מהתשובה הנכונה, אך במקרה הזה המדידה נעשית בצורה הסתברותית, על פני כל הטוקנים האפשריים.
</Callout>

## אז איך יודעים איזה פרמטר להזיז?

כאן מגיעה הבעיה האמיתית. כשיש לך טריליון פרמטרים, אי אפשר לנסות כל אחד בנפרד ולראות מה קורה.

צריך לדעת, לכל פרמטר בנפרד "אם אזיז אותך קצת, הטעות תגדל או תקטן? ובכמה?"

זה בדיוק מה שגרדיאנט אומר. הגרדיאנט של ה-loss ביחס לפרמטר  הוא:

$$
\frac{\partial L}{\partial w}
$$

<Explainer term="גרדיאנט">
  הגרדיאנט הוא הנגזרת של ה-loss ביחס לפרמטר מסוים.
  במילים פשוטות, נגזרת אומרת לנו כמה שינוי קטן בדבר אחד משפיע על דבר אחר.

  במקרה שלנו, השאלה היא: “אם אגדיל את הפרמטר הזה בטיפה, ה-loss יעלה או ירד, ובכמה?”
</Explainer>

- גרדיאנט **חיובי** - הגדלת $w$ תגדיל את הטעות, אז כדאי להקטין אותו.
- גרדיאנט **שלילי** - הגדלת $w$ תקטין את הטעות, אז כדאי להגדיל אותו.
- גרדיאנט **אפס** - הפרמטר לא משפיע כרגע על הטעות.

ועכשיו נוצרת בעיה חדשה מכיוון שרשת עמוקה היא פונקציה של פונקציות של פונקציות. אז איך מחשבים גרדיאנט דרך כל השכבות האלה?

## Backpropagation - כלל השרשרת בפעולה

הפתרון מגיע מחדו"א: **כלל השרשרת**.

אם $L$ תלוי ב-$\hat{y}$, וב-$\hat{y}$ תלוי ב-$w$, אז:

$$
\frac{\partial L}{\partial w} = \frac{\partial L}{\partial \hat{y}} \cdot \frac{\partial \hat{y}}{\partial w}
$$

אפשר לפרק גרדיאנט מורכב לשרשרת של גרדיאנטים פשוטים יותר. backprop הוא בדיוק זה. מתחיל מה-loss בסוף ומגלגל אחריות אחורה, שכבה אחרי שכבה, עד שכל פרמטר יודע את הגרדיאנט שלו.

<Explainer term="כלל השרשרת">
  כלל מחדו"א לגזירת פונקציה של פונקציה: הנגזרת של $f(g(x))$ היא $f'(g(x)) \cdot g'(x)$. כשרשת היא ערימה של פונקציות זו על גבי זו, הכלל הזה מאפשר לפרק את הגרדיאנט הכולל למכפלה של גרדיאנטים מקומיים אחד לכל שכבה.
</Explainer>

> **אינטואיציה:** ה-loss בסוף "מגלגל אחריות" אחורה. הוא שואל את השכבה שלפניו "כמה אתה אשם בטעות?", זו שואלת את שלפניה, וכך הלאה עד הפרמטר הראשון — וכל אחד מקבל בדיוק את חלקו בטעות.

**דוגמה מספרית:**

רשת זעירה עם **שתי שכבות**, ו**פרמטר אחד בכל שכבה**: $w_1$ לוקח את הקלט $x$ ומייצר ערך ביניים . זו **שכבה נסתרת**. אחר כך $w_2$ לוקח את $z$ ומייצר את הפלט $\hat{y}$ שהיא שכבת הפלט. בדיוק המבנה הרב-שכבתי הזה הוא שנותן ל-backprop "דרך" לגלגל את האחריות אחורה, שכבה אחרי שכבה:

<Explainer term="שכבה נסתרת">
  שכבה ש"יושבת באמצע", הערכים שלה ($z$ במקרה הזה) אינם הקלט ואינם הפלט הסופי, אלא ייצוג ביניים שהרשת בונה לעצמה בדרך. בדוגמה הזו יש שכבה נסתרת אחת בלבד; ברשתות אמיתיות מערימים עשרות ומאות כאלה וזה בדיוק מה שהופך אותן ל"עמוקות". על שכבות, נוירונים ועומק נרחיב בפוסט נפרד.
</Explainer>

$$
x \xrightarrow{\times w_1} z = w_1 \cdot x \xrightarrow{\times w_2} \hat{y} = w_2 \cdot z \longrightarrow L = (\hat{y} - y)^2
$$

נניח: $x=2,\ w_1=3,\ w_2=0.5,\ y=6$

<BackpropDiagram />

**Forward pass:**

$$
\begin{aligned}
z &= w_1 \cdot x = 3 \cdot 2 = 6 \\
\hat{y} &= w_2 \cdot z = 0.5 \cdot 6 = 3 \\
L &= (\hat{y} - y)^2 = (3 - 6)^2 = 9
\end{aligned}
$$

**Backward pass מגלגלים אחורה:**

קודם כל, הגרדיאנט של ה-loss ביחס לפלט:

$$
\frac{\partial L}{\partial \hat{y}} = 2(\hat{y} - y) = 2(3-6) = -6
$$

עכשיו $w_2$. כלל השרשרת מכפיל שתי נגזרות מקומיות: את $\frac{\partial L}{\partial \hat{y}} = 2(\hat{y}-y)$ שכבר חישבנו, ואת $\frac{\partial \hat{y}}{\partial w_2} = z$ (כי $\hat{y} = w_2 \cdot z$). קודם כותבים את הנגזרות, ורק אז מציבים מספרים:

$$
\begin{aligned}
\frac{\partial L}{\partial w_2}
&= \frac{\partial L}{\partial \hat{y}} \cdot \frac{\partial \hat{y}}{\partial w_2} \\
&= 2(\hat{y}-y) \cdot z \\
&= 2(3-6) \cdot 6 = -36
\end{aligned}
$$

ובשביל $w_1$ צריך לעבור דרך $z$, אז יש שלוש נגזרות מקומיות. בנוסף לקודמת: $\frac{\partial \hat{y}}{\partial z} = w_2$ (כי $\hat{y} = w_2 \cdot z$) ו-$\frac{\partial z}{\partial w_1} = x$ (כי $z = w_1 \cdot x$). שוב — קודם הנגזרות, ואז המספרים:

$$
\begin{aligned}
\frac{\partial L}{\partial w_1}
&= \frac{\partial L}{\partial \hat{y}} \cdot \frac{\partial \hat{y}}{\partial z} \cdot \frac{\partial z}{\partial w_1} \\
&= 2(\hat{y}-y) \cdot w_2 \cdot x \\
&= 2(3-6) \cdot 0.5 \cdot 2 = -6
\end{aligned}
$$

שני הגרדיאנטים שליליים, כלומר הגדלת שניהם תקטין את הטעות. הגיוני, כי המודל ניחש 3 והתשובה הייתה 6.

<BackpropGraph client:visible />

## Gradient Descent השימוש בגרדיאנט לכיוון הפרמטרים

לפני שנשתמש בגרדיאנט, כדאי להבין מה אנחנו בעצם מחפשים.

כל צירוף של פרמטרים נותן loss אחר. פרמטרים מסוימים יגרמו למודל לטעות הרבה, אחרים פחות. המטרה היא למצוא את הצירוף שמביא את ה-loss לכי נמוך שאפשר, כי שם המודל טועה הכי מעט. אלה בדיוק "הערכים הנכונים" שחיפשנו מתחילת הפוסט.

הבעיה: יש טריליון פרמטרים. אי אפשר לנסות כל צירוף ולבדוק איזה עובד. וכאן הגרדיאנט נכנס לתמונה. $\frac{\partial L}{\partial w}$ ​ אומר בדיוק שני דברים:
1\. **כמה השפעה** יש לפרמטר$w$ על ה-loss. גרדיאנט גדול אומר שהפרמטר הזה אחראי לחלק משמעותי מהטעות.
2\. **לאיזה כיוון לעדכן,** גרדיאנט חיובי אומר שהגדלת $w$תגדיל את הטעות. שלילי אומר שתקטין אותה.

אז במקום לנסות כל צירוף, שואלים בכל נקודה: לאיזה כיוון ה-loss עולה? ומזיזים בכיוון ההפוך, צעד קטן כל פעם. זה gradient descent.

כלל העדכון פשוט:

$$
w \leftarrow w - \eta \cdot \frac{\partial L}{\partial w}
$$

כאשר $\eta$ (eta) הוא ה-**learning rate**, כמה גדול כל צעד.

<Explainer term="learning rate">
  גודל הצעד בכל עדכון, מסומן $\eta$. גדול מדי מדלגים מעבר למינימום ומתנדנדים סביבו, או מתבדרים לגמרי. קטן מדי מתקדמים למינימום בזחילה. למצוא את הערך הנכון זו חלק מהאומנות של אימון מודלים.
</Explainer>

למה להחסיר? כי אנחנו רוצים לרדת בכיוון ההפוך לגרדיאנט. אם הגרדיאנט חיובי, $w$ גדול מדי, אז נקטין אותו.

למה לא לזוז הרבה בבת אחת? כי צעד גדול מדי יכול לדלג מעל המינימום ולהחמיר את המצב. learning rate גבוה מדי, המודל מתנדנד ולא מתכנס. נמוך מדי, האימון לוקח נצח.

> **אינטואיציה:** דמיינו שאתם יורדים במורד הר בערפל כבד. אתם לא רואים את התחתית, אבל מרגישים את השיפוע מתחת לרגליים וצועדים בכל פעם קצת בכיוון היורד. זה בדיוק gradient descent.

<LossLandscape client:visible />

וברמת המאקרו, אחרי שמריצים את הלולאה אלפי פעמים, ה-loss פשוט יורד. ככה זה נראה לאורך אימון אמיתי:

<LossCurve client:visible series={[{ key: 'loss', label: 'loss', color: 'var(--color-accent)' }]} data={Array.from({ length: 40 }, (_, i) => ({ step: i * 100, loss: +(2.4 * Math.exp(-i / 9) + 0.18).toFixed(3) }))} />

## Optimizer מי מחליט כמה לזוז

gradient descent בצורתו הפשוטה אומר לכל פרמטר לזוז בדיוק לפי הנוסחה שראינו. זה נקרא **SGD** (Stochastic Gradient Descent).

אבל בפועל, רוב המודלים משתמשים ב-optimizer חכם יותר כמו **Adam**. Adam לא מסתכל רק על הגרדיאנט הנוכחי. הוא מסתכל על ההיסטוריה של הגרדיאנטים ומתאים את גודל הצעד לכל פרמטר בנפרד. פרמטרים שהגרדיאנט שלהם עקבי מקבלים צעדים גדולים יותר. פרמטרים שהגרדיאנט שלהם קופץ לכל כיוון, צעדים קטנים יותר.

<Explainer title="SGD מול Adam — מה ההבדל בקצרה?">
  **SGD** מזיז כל פרמטר בדיוק לפי הגרדיאנט הנוכחי, באותו גודל צעד לכולם. **Adam** שומר "זיכרון" של הגרדיאנטים האחרונים ומתאים גודל צעד נפרד לכל פרמטר' מאיץ בכיוונים יציבים, מאט בכיוונים שמתנדנדים. בפועל הוא מתכנס מהר יותר, ולכן הוא מה שמאמן את רוב המודלים הגדולים.
</Explainer>

כדי להרגיש את ההבדל: לחצו על המשטח כדי להפיל נקודת התחלה, והשוו איך SGD, Momentum ו-Adam מתקדמים לאותו מינימום. המשטח כאן דו-ממדי ויש בו כמה מינימומים, אז גם נקודת ההתחלה משנה:

<GradientDescent client:visible />

על SGD, Adam, ומה שביניהם ארחיב בהמשך :).

## אז לסיכום מה זה Pretraining ו-Finetuning

כל מה שראינו עד עכשיו, אותה לולאה בדיוק. ההבדל הוא רק בדאטה.

**Pretraining** הוא האימון הראשוני. מריצים את הלולאה על כמויות ענק של טקסט מהאינטרנט, ספרים, אתרים, קוד, מאמרים. המטרה פשוטה: לנחש את המילה הבאה בטקסט (ליתר דיוק, את ה[טוקן](/posts/why-hebrew-costs-more) הבא). מיליארדי דוגמאות, שבועות של חישוב על אלפי GPU.

מה המודל לומד בשלב הזה? לא עובדות במפורש, אלא משהו עמוק יותר. איך מילים מתקשרות אחת לשנייה, מה הסמנטיקה של שפה, איך רעיונות בנויים. בסוף יש מודל שמבין שפה ועולם, אבל עוד לא ברור איך לשוחח איתו.

**Finetuning** הוא שלב שני. לוקחים את המודל הזה ומריצים שוב את אותה לולאה, הפעם על דאטה קטן וממוקד. ChatGPT הוא דוגמה טובה לשני סוגים של finetuning שנעשו עליו.

כאשר הראשון נקרא **instruction tuning**, אימון על אלפי דוגמאות של שיחות בפורמט "משתמש שואל, עוזר עונה". זה מה שגרם לו לדעת שכשיש User הוא אמור להתנהג כעוזר ולא כמנוע השלמת טקסט.

השני הוא **RLHF**, שעליו הרחבתי [בפוסט על mode collapse](/posts/mode-collapse-and-the-goblin-problem). בקצרה, במקום דוגמאות טקסט, הדאטה הוא משוב ישיר מבני אדם על איכות התשובות. זה מה שכיוון את ההתנהגות, הנימוס, הגבולות.

אותה לולאה בדיוק בכל השלבים. הדאטה הוא שמשתנה.

<Callout type="tip" title="למה לא מאמנים מאפס בכל פעם?">
  כי pretraining עולה מיליוני דולרים ולוקח שבועות. finetuning עולה הרבה פחות, כי מתחילים ממודל שכבר יודע המון — רק מכווננים אותו. זאת הסיבה שכמעט אף אחד לא מאמן מודל בסיס בעצמו, אלא בונה מעל מודלים קיימים.
</Callout>

<PretrainFinetune client:visible />

## זה הכל

אימון, pretraining, finetuning. כולם וריאציות על אותה לולאה. forward pass, מדידת loss, backprop שמחשב גרדיאנטים דרך כלל השרשרת, gradient descent שמעדכן את הפרמטרים.

<PullQuote>
  כל "אימון" הוא בסוף אותה לולאה: לנחש, למדוד כמה טעית, ולזוז טיפה בכיוון הנכון. שוב, ושוב, מיליארדי פעמים.
</PullQuote>

לקח לי יותר מדי זמן לגלות שזה פשוט כל כך.